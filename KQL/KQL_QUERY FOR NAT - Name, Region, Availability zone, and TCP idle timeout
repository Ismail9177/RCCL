resources
| where type =~ "microsoft.network/natgateways"
| project
    Name = name,
    Region = location,
    AvailabilityZones = tostring(properties.zones),          
    TcpIdleTimeoutInMinutes = tostring(properties.idleTimeoutInMinutes)
